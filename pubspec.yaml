name: HealthPal
description: A new Flutter profile project.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  supabase_flutter: ^2.10.3
  url_launcher: ^6.3.2
  http: ^1.6.0
  mime: ^2.0.0
  web_socket_channel: ^3.0.3
  intl: ^0.19.0
  image_picker: ^1.2.1

  # MAP & LOCATION (gratis)
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  geolocator: ^10.1.0

  flutter_markdown: ^0.7.3 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
